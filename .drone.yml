workspace:
  base: /go
  path: src/github.com/lovoo/goka

debug: true

pipeline:
  build:
    image: golang:${GO_VERSION}
    commands:
      - go get -v -t ./...
      - go test ./...

matrix:
  GO_VERSION:
    - 1.7
    - 1.8
    - latest
